PHỞ NHÀ ANNA – V11 FIXED VERIFICATION

Base: V10 FIXED package.

Reported defects addressed:
1) ADMIN F5/session persistence
   - Added persistent random server-backed admin_sessions in D1.
   - Login creates a 7-day random session token.
   - HttpOnly Secure SameSite=Lax cookie is restored on F5.
   - localStorage/sessionStorage token is also sent as a fallback.
   - Legacy sha256(password) token remains accepted for old admin pages.
   - Password change creates a fresh session instead of returning the obsolete password hash.

2) ADMIN "MỞ KHÓA THÊM LƯỢT" server error
   - Added dedicated admin_extra_unlocks table.
   - This avoids legacy customer_unlocks CHECK constraints/schema differences.
   - Admin unlock is idempotent per customer/day and never double-adds.
   - Customer register/spin reads the dedicated extra-unlock state.
   - Unlock claim type 4 also uses the dedicated table.
   - Delete customer / delete-all cleanup removes extra unlock records.

3) Customer turn synchronization
   - public/index.html polls /api/register every 5 seconds and on visibility return.
   - Server remains the source of truth for used/remaining turns.

4) Consecutive winning spins
   - Server checks the two immediately previous global plays.
   - If both are winning prize indexes 0-4, current result is forced to prize index 5.

5) Concurrent spin protection
   - D1 global spin lock serializes spin requests.

Static verification:
- node --check src/worker.js: PASS
- JavaScript in public/admin.html: PASS
- JavaScript in public/index.html: PASS
- ZIP integrity: PASS
