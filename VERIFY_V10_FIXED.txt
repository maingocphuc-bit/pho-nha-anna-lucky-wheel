PHỞ NHÀ ANNA – V10 FIXED VERIFICATION

Source base: PHO_NHA_ANNA_LUCKY_WHEEL_V10_FINAL_STABLE.zip

Verified fixes added in this package:
1) Extra-turn sync: public/index.html polls /api/register every 5s and on visibility return,
   so admin-granted turns are reflected without F5.
2) Consecutive wins: /api/spin checks the two latest plays globally; if both are winning
   prize indexes 0-4, the current result is forced to prize index 5 (consolation).
3) Concurrent spin safety: D1 spin_locks table + global lock serializes spin requests.
4) Admin F5 session: existing HttpOnly cookie + /api/admin/session + localStorage fallback
   are preserved; bootAdmin keeps the admin shell on transient errors and only clears session
   after an explicit 401.

Syntax check: src/worker.js passed `node --check`.
